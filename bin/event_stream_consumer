#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/perp_liquidation'

application = PerpLiquidation::Application.new
consumer = application.event_stream_consumer
raise 'EVENT_STREAMS_ENABLED must be true' unless consumer

loop { consumer.poll }
