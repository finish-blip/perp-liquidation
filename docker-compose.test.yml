name: perp-liquidation-test

services:
  rspec:
    build:
      context: .
      dockerfile: Dockerfile.real
    network_mode: none
    working_dir: /workspace
    volumes:
      - type: bind
        source: .
        target: /workspace
        read_only: true
    entrypoint:
      - bundle
      - exec
      - rspec
